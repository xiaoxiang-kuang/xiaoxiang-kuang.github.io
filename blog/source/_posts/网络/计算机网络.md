---
title: 计算机网络
categories:
  - [网络]
site: 网络
date: 2021-08-27 16:22:59
---

* 因特网是一个世界范围的计算机网络，互联了遍及全世界的数以亿计的计算设备的网络。
* 主机（host）：（端系统）与因特网相连的计算机和其他设备。
* 主机通过通信链路（电缆、光纤等）和分组交换机（路由器、链路层交换机）连接到一起。当一台主机向另一台主机发送数据，发送主机将数据分组（将数据分段，每段加上首部字节）。分组交换机从他的一条入通信链路接收到达的分组，从它的出通信链路转发该分组。路由器通常位于网络核心，链路层交换机位于接入网中。
* ISP(Internet Service Provider)：因特网服务提供商。主机通过ISP介入到因特网，每个交换机由多个通信链路和分组交换机组成。
* IETE(Internet Engineering Task Force, IETE)：因特网工程任务组。
* RFC(Request For Comment)：请求评论，定义了TCP、IP、HTTP、SMTP等协议。
* **协议**：定义了在两个或多个通信实体之间交换报文的格式和次序，以及报文发送或接收等事件采取的动作。
* **报文（message）**：是网络中交换与传输的数据单元，即站点一次性要发送的数据块。通常将长报文划分为较小的数据块，称之为分组（packet）。分组等于通信链路的最大传输速率。
* 边缘路由器（edge router）：是指端系统到任何其他远程系统的路径上的第一台路由器。
* 接入网（access network）：是指将端系统连接到其边缘路由器的物理链路。
* DSL(Digital Subscriber Line)：数字电话线。
* 物理媒体分为两类：导引型媒体（光缆、双绞线、同轴电缆）和非导引型媒体（电波在空气、外层空间中传播，如无线局域网或数字卫星频道）。
* 物理链路的实际成本很小，但是安装物理链路的劳动力成本比材料成本高好几个数量级，所以许多建筑商在一个建筑物的每个房间中都安装了双绞线、光缆等。
* 双绞铜线：一百年来一直用于电话网。双绞线由两个隔离的铜线组成，每根大约1mm粗，以规则的螺线形式排列。两根线被绞合起来，以减少电气干扰，通常使用4对双绞线捆在一起形成一根电缆，并在外面覆盖上保护性的防护层。就是带水晶头的网线。
* 同轴电缆：由两个铜导体组成，但这两个导体是同心的。常用于有线电视。
* 光纤：细而柔软、能够引导光脉冲的媒体。每个脉冲表示一个比特。
